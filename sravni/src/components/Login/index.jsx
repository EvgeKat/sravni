import React from 'react';
import styles from './Login.module.css';

const Login = () => {
    return (
        <div className={styles.conteiner}>
         <button className={styles.conteiner}>Войти</button>
        </div>
    );
}

export default Login;